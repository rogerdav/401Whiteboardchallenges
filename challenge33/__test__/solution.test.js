'use strict';
const  = require('../lib/solution');
require('jest');

describe('the iterative approach', () => {

  it('should return number', () => {
    expect().toBe();
  });
  it('should return number', () => {
    expect().toEqual();
  });

  it('should return number', () => {
    expect().toBe();
  });
  it('should return number', () => {
    expect().toEqual();
  });

});